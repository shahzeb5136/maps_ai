# ── Web service ──────────────────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
asyncpg>=0.30,<0.31
PyJWT[crypto]>=2.9,<3.0
python-dotenv>=1.0,<2.0

# ── Scanner pipeline ─────────────────────────────────────────────────────────
google-genai>=2.0,<3.0
requests>=2.32,<3.0
pillow>=11.0,<14.0
pydantic>=2.9,<3.0
streetview>=0.1.0,<0.2

# ── PDF ──────────────────────────────────────────────────────────────────────
# Pure Python: no headless browser, no system libraries, no apt step.
reportlab>=4.2,<5.0
